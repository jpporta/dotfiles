#!/bin/env bash

POKEDEX=$HOME/.local/state/got-pokemons.txt

POKEMON=$(pokeget random 2>&1 >/dev/null)

if ! grep -q "$POKEMON" $POKEDEX; then
		echo -ne "$(date +%s)\t$(echo -n $POKEMON)\n" >> $POKEDEX
fi

pokeget $POKEMON 2>/dev/null
